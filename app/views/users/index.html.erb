<div class="container">
    <h1 align="center" class="section-title">Directory</h1>
    <% if logged_in? %>
        <table class="table">
            <thead>
                <th>Last Name</th>
                <th>First Name</th>
                <th>Email Address</th>
                <th>Phone Number</th>
                <th></th>
            </thead>
            <tbody>
                <% @users.each do |user| %>
                    <tr>
                        <td><%= user.lastname %></td>
                        <td><%= user.firstname %></td>
                        <td><%= user.email %></td>
                        <td><%= user.phone_number %></td>
                        <% if logged_in? %>
                            <% if user.id == current_user.id %>
                                <td><%= link_to "edit profile", edit_user_path(user), class: "btn btn-info btn-sm btn-block" %></td>
                            <% end %>
                        <% end %>
                    </tr>
                <% end %>
            </tbody>
        </table>
    <% else %>
        <div class="user-not-allowed">
            <p>You must be <%= link_to "logged in", login_path %> to view the directory.</p>
        </div>
    <% end %>
</div>